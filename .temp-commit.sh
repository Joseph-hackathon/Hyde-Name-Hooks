#!/usr/bin/env bash
git commit -m "$(cat <<'EOF'
feat: refresh branding and landing story

Update typography, hero copy, and card flows across the frontend and
apply the new Hyde logo asset to the global header.
EOF
)"
